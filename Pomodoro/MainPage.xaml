<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="Pomodoro.MainPage">

    <StackLayout BackgroundColor="#ff3f4b">
        <Frame BackgroundColor="#ff6363" Padding="24" CornerRadius="0">
            <Label Text="Welcome to PomodoroTimer!" HorizontalTextAlignment="Center" TextColor="White" FontSize="36"/>
        </Frame>
        <Label TextColor="White" HorizontalTextAlignment="Center" Text="Нажмите на помидор для старта:" FontSize="Title" Padding="30,10,30,10"/>
        <!--
        <Label Text="Make changes to your XAML file and save to see your UI update in the running app with XAML Hot Reload. Give it a try!" FontSize="16" Padding="30,0,30,0"/>
        <Label FontSize="16" Padding="30,24,30,0">
            <Label.FormattedText>
                <FormattedString>
                    <FormattedString.Spans>
                        <Span Text="Learn more at "/>
                        <Span Text="https://aka.ms/xamarin-quickstart" FontAttributes="Bold"/>
                    </FormattedString.Spans>
                </FormattedString>
            </Label.FormattedText>
        </Label>
        -->
        <ImageButton HorizontalOptions="Center" BackgroundColor="#ff3f4b" Source="https://clck.ru/32khLV">
            
        </ImageButton>
    </StackLayout>

</ContentPage>
